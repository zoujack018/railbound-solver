# Railbound 求解器 (Worker Code) 代码结构与各部分意义解析

`railbound-worker-code.js` 文件是 Railbound 游戏逻辑的核心求解引擎。它被设计为一个运行在 Web Worker 中的后台任务，负责接收关卡数据并计算出放置轨道的过关方案。代码结构非常清晰，主要分为以下几个部分：

## 1. 基础常量与预处理 (第 7 - 24 行)
从 `railbound-logic.js` 导入基础数据结构（不同轨道的定义、方向常量、偏移量 `DELTA` 等）。并在初始化时建立各类轨道的入口/出口索引映射表 (`TRACKS_BY_ENTRY`, `BASIC_BY_ENTRY`, `TRACKS_BY_EXIT`)，方便后续快速查找哪些轨道能够连接特定方向。

## 2. 地图元素构建与状态管理 (第 26 - 114 行)
包含一系列 `buildXxxMap` 的函数，用于将关卡数据中的交互元素转化为方便查询的字典（Map）结构。
* **隧道 (Tunnels)**、**T型道岔 (T-Switches)**、**自动道岔 (Auto-Switches)**、**平台/乘客 (Platforms)**。
* 还有处理道岔翻转状态 (`effectiveTrackAt`)、计算阻拦门 (`barriers`) 占用情况等辅助函数。这些构成了模拟器运行时的环境状态。

## 3. 寻路预剪枝与 0号车前瞻 (第 115 - 262 行)
* **`zeroSafetyLookahead` (0号车安全检查)**：用于检测特殊的 0 号列车（无限循环或触发机制的列车）会不会发生碰撞或者错误进站。
* **`forwardReachable` 与 `backwardReachable`**：分别从起点正向寻路和从终点逆向寻路。
* **`filterBlanks`**：结合正逆向寻路结果，提前剔除掉那些列车绝对不可能经过的“死胡同”空白格子，极大缩小后续求解过程中的搜索空间 (Search Space)。

## 4. 核心模拟引擎 (`simulate`) (第 264 - 339 行)
模拟列车在给定轨道上运行的完整过程。它按步数（step）推进时间：
* 处理车辆的移动、出入隧道、经过道岔翻转。
* 处理开关（Triggers）和阻挡门（Barriers）的联动。
* 检查追尾、对撞等各种碰撞。
* 校验列车是否按照正确的顺序（`order`）到达终点，以及是否接上了所有的乘客。
这是判断某一套轨道摆放方案是否合法的**“绝对标准”**。

## 5. CSP 单车路径枚举 (`v4: BASIC-ONLY path enumeration`) (第 341 - 605 行)
这部分是为了 CSP（约束满足问题）求解器做准备的。
* **`bfsMinSteps` & `buildHeuristic`**：使用广度优先和启发式算法（A*），计算出列车到达终点的理论最短距离。
* **`enumeratePaths`**：为**每一辆车单独**进行深度优先搜索，穷举出它从起点到终点所有可行的单车路径（忽略其他车的存在，只考虑经过乘客点的顺序）。

## 6. CSP 组合求解器 (`solveCSP`) (第 607 - 957 行)
* 接收前面枚举出的一大堆“单车路径”，然后尝试将它们**组合（Merge）**在一起。
* 当两条不同车的路径在同一个格子相交时，尝试将两个基础轨道（Basic Tracks）合并为 T 型交叉口（T-Tracks）。
* 通过束搜索（Beam Search）和回溯算法（Backtracking），它能非常快地解出大部分需要合并轨道的常规关卡。但由于它在枚举单车路径时无法准确预测机关的状态，因此对于复杂的机关关卡它是“不完备”的。

## 7. DFS 深度优先求解器 (`solveDFS`) (第 959 - 1199 行)
* 这是一个传统的、基于网格的穷举求解器。它一步步地在空白格子上尝试放置轨道，或者把已有的轨道升级成 T 型交叉口。
* 每次尝试放置后，都验证状态合法性。它的速度比 CSP 慢很多，但它是**完备的**，能够准确处理自动道岔、触发器等所有复杂的动态机关。通常在 CSP 求解失败、或者关卡包含复杂机关时作为备用（Fallback）被调用。

## 8. Worker 通信入口 (`self.onmessage`) (第 1201 - 1289 行)
* 接收主线程传来的解谜请求（关卡 JSON）。
* 根据关卡元素的复杂度，智能决定调度策略：一般会先使用 `solveCSP` 尝试快速求解，如果关卡内包含复杂的动态机关（如自动道岔、带状态的阻挡门），或者 CSP 失败，则自动降级（Fallback）调用 `solveDFS` 进行兜底计算。
* 将最终找出的解答（或者找不到解答的结果）通过 `postMessage` 返回给主线程渲染。
